我现在要开始实现 wiki skill 的步骤3、文件结构生成

这个步骤有 2 个成员协作完成
执行流程：
1、检查 modules.json,progress.json 的状态
- 如果 modules.json 的 merge_modules.pending_modules、merge_modules.progress_modules 还存在数据
- 如果 progress.json 的 status 不是 "completed"
2、创建团队、召唤 file-generator 成员
3、收到 file-generator 反馈的结果、执行后续的操作
- 接收到处理完毕、关闭 file-generator 当前成员
主意：
- file-generator 只能处理 modules.json 中一项的数据
- 这是为了保持 file-generator 的上下文用量、和超高的处理水准
当 file-generator 处理完一项数据后、检查 modules.json 是否还有需要处理的数据
如果还有、回到 第二部（召唤 file-generator 成员）继续处理
4、modules.json 处理完毕、更改 progress.json 的状态

1、主成员team-lead
- 协调 file-generator

2、文件生成file-generator
- 获取 modules.json 数据、根据模块的划分来生成文件结构数据、并同时记录缓存数据到 progress.json
- 他是每次都获取 modules.json 的一项数据、并根据这一项数据生成文件结构
- 他在执行前需要把 modules.json 获取的一项数据移动到 merge_modules.progress_modules 字段中(这一步可以在 module-manager.js) 脚本中执行、并同时更改
"total_modules": 33,
"pending_modules": 33,
"progress_modules": 0,
"completed_modules": 0,
这几个字段的值
- 生成文件到 progress.json 中、生成完毕后进入审批环节
记住：
- 需要强制严格的审批环节、这决定后续 wiki 文档的质量、他需要对这个环节负责主要责任

审批结果：
- 如果审批通过、通知 主成员team-lead 协调工作
- 如果审批失败、并根据 file-reviewer 的补充建议并再次继续生成、直到审批通过

需要改造的关联文件
- .claude\skills\wiki\SKILL.md
- .claude\skills\wiki\agents\file-generator.md
- .claude\skills\wiki\scripts\module-manager.js
- .claude\skills\wiki\scripts\progress-manager.js

需要创建新的文件
- .claude\skills\wiki\references\data-structures\progress.md